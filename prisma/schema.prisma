// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql" // หรือ mysql
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// 1. User Table
model User {
  id            String    @id @default(uuid())
  email         String    @unique
  name          String?
  password      String
  role          Role      @default(USER)
  createdAt     DateTime  @default(now())
  
  // Profile Info
  image         String?
  phone         String?
  dateOfBirth   DateTime?
  // address    String? @db.Text  <-- ลบอันเก่าออก (ไม่ใช้แล้ว)

  // Relations
  addresses     Address[] // 🔥 เชื่อมกับตารางที่อยู่ใหม่
  builds        Build[]
  orders        Order[]
  favorites     UserFavorite[]
  reviews       Review[]
}

enum Role {
  USER
  ADMIN
}

// 🔥 2. Address Table (ใหม่: เก็บที่อยู่ละเอียด)
model Address {
  id          String   @id @default(uuid())
  name        String   // ชื่อผู้รับ
  phone       String   // เบอร์ติดต่อ
  
  // ที่อยู่แบบละเอียด
  houseNumber String   // บ้านเลขที่, หมู่, ซอย, ถนน
  subdistrict String   // ตำบล/แขวง
  district    String   // อำเภอ/เขต
  province    String   // จังหวัด
  zipcode     String   // รหัสไปรษณีย์
  
  isDefault   Boolean  @default(false)

  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  createdAt   DateTime @default(now())
}

// 3. Product Table
model Product {
  id          String   @id @default(uuid())
  name        String
  description String?
  price       Decimal  @db.Decimal(10, 2)
  stock       Int      @default(0)
  image       String?
  category    Category
  specs       Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  orderItems  OrderItem[]
  buildItems  BuildItem[]
  favoritedBy UserFavorite[]
  reviews     Review[]
}

enum Category {
  CPU
  MOTHERBOARD
  RAM
  GPU
  STORAGE
  PSU
  CASE
  COOLER
  MONITOR
  LAPTOP
  DESKTOP
  MOUSE
  KEYBOARD
  HEADSET
  CHAIR
  ACCESSORY
  ALL
}

// 4. Build Table
model Build {
  id          String      @id @default(uuid())
  name        String
  totalPrice  Decimal
  userId      String
  user        User        @relation(fields: [userId], references: [id])
  items       BuildItem[]
  createdAt   DateTime    @default(now())
}

model BuildItem {
  id        String  @id @default(uuid())
  buildId   String
  build     Build   @relation(fields: [buildId], references: [id], onDelete: Cascade)
  productId String
  product   Product @relation(fields: [productId], references: [id])
  quantity  Int     @default(1)
}

// 5. Order Table (อัปเดตใหม่ 🔥)
model Order {
  id        String      @id @default(uuid())
  userId    String
  user      User        @relation(fields: [userId], references: [id])
  
  status    OrderStatus @default(PENDING)
  total     Decimal
  items     OrderItem[]
  createdAt DateTime    @default(now())

  // Logistics
  trackingNumber  String?
  carrier         String?
  
  // Address Snapshot
  shippingName    String?
  shippingPhone   String?
  shippingAddress String?
  shippingZipcode String?

  // 🔥 Payment Info (เพิ่มส่วนนี้)
  slipImage       String?   // URL รูปสลิปโอนเงิน
}

enum OrderStatus {
  PENDING     // รอชำระเงิน
  VERIFYING   // 🔥 รอตรวจสอบ (ลูกค้าแนบสลิปแล้ว)
  PAID        // ชำระเงินแล้ว (แอดมินคอนเฟิร์ม)
  SHIPPED     // ส่งสินค้าแล้ว
  COMPLETED   // สำเร็จ
  CANCELLED   // ยกเลิก
}

model OrderItem {
  id        String  @id @default(uuid())
  orderId   String
  order     Order   @relation(fields: [orderId], references: [id])
  productId String
  product   Product @relation(fields: [productId], references: [id])
  quantity  Int
  price     Decimal
}

// 6. Favorites Table
model UserFavorite {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  productId String
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([userId, productId])
}

// 7. Review Table
model Review {
  id        String   @id @default(uuid())
  rating    Int      @default(5)
  comment   String?  @db.Text
  images    String[]
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  productId String
  product   Product  @relation(fields: [productId], references: [id])
  createdAt DateTime @default(now())
}
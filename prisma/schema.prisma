// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql" // หรือ mysql
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// 1. ตาราง User
model User {
  id          String    @id @default(uuid())
  email       String    @unique
  name        String?
  password    String
  role        Role      @default(USER)
  createdAt   DateTime  @default(now())
  
  // Profile Info
  image       String?
  phone       String?
  dateOfBirth DateTime?
  // address   String?  @db.Text // ❌ อันเก่า (เลิกใช้)

  // Relations
  addresses Address[]   // 🔥 ความสัมพันธ์กับตารางที่อยู่ใหม่
  builds    Build[]
  orders    Order[]
  favorites UserFavorite[]
  reviews   Review[]
}

enum Role {
  USER
  ADMIN
}

// 🔥 2. ตารางที่อยู่ (ใหม่)
model Address {
  id          String   @id @default(uuid())
  name        String   // ชื่อผู้รับ
  phone       String   // เบอร์ติดต่อ
  
  // ที่อยู่ละเอียด
  houseNumber String   // บ้านเลขที่, หมู่, ซอย, ถนน
  subdistrict String   // แขวง/ตำบล
  district    String   // เขต/อำเภอ
  province    String   // จังหวัด
  zipcode     String   // รหัสไปรษณีย์
  
  isDefault   Boolean  @default(false) // เป็นที่อยู่หลักหรือไม่

  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  createdAt   DateTime @default(now())
}

// 3. ตารางสินค้า
model Product {
  id          String   @id @default(uuid())
  name        String
  description String?
  price       Decimal  @db.Decimal(10, 2)
  stock       Int      @default(0)
  image       String?
  category    Category
  specs       Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  orderItems  OrderItem[]
  buildItems  BuildItem[]
  favoritedBy UserFavorite[]
  reviews     Review[]
}

enum Category {
  CPU
  MOTHERBOARD
  RAM
  GPU
  STORAGE
  PSU
  CASE
  COOLER
  MONITOR
  LAPTOP
  DESKTOP
  MOUSE
  KEYBOARD
  HEADSET
  CHAIR
  ACCESSORY
  ALL
}

// 4. ตารางจัดสเปค
model Build {
  id          String      @id @default(uuid())
  name        String
  totalPrice  Decimal
  userId      String
  user        User        @relation(fields: [userId], references: [id])
  items       BuildItem[]
  createdAt   DateTime    @default(now())
}

model BuildItem {
  id        String  @id @default(uuid())
  buildId   String
  build     Build   @relation(fields: [buildId], references: [id], onDelete: Cascade)
  productId String
  product   Product @relation(fields: [productId], references: [id])
  quantity  Int     @default(1)
}

// 5. ตารางคำสั่งซื้อ (อัปเดต)
model Order {
  id        String      @id @default(uuid())
  userId    String
  user      User        @relation(fields: [userId], references: [id])
  status    OrderStatus @default(PENDING)
  total     Decimal
  items     OrderItem[]
  createdAt DateTime    @default(now())

  // 🔥 Logistics Info (ใหม่)
  trackingNumber  String? // เลขพัสดุ
  carrier         String? // บริษัทขนส่ง (Kerry, Flash, EMS)
  
  // 🔥 Snapshot ที่อยู่จัดส่ง (เก็บแยกเผื่อ User เปลี่ยนที่อยู่ทีหลัง)
  shippingName    String?
  shippingPhone   String?
  shippingAddress String? // เก็บรวม houseNumber + subdistrict...
  shippingZipcode String?
}

enum OrderStatus {
  PENDING
  PAID
  SHIPPED
  COMPLETED
  CANCELLED
}

model OrderItem {
  id        String  @id @default(uuid())
  orderId   String
  order     Order   @relation(fields: [orderId], references: [id])
  productId String
  product   Product @relation(fields: [productId], references: [id])
  quantity  Int
  price     Decimal
}

// 6. ตารางรายการโปรด
model UserFavorite {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  productId String
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([userId, productId])
}

// 7. ตารางรีวิว
model Review {
  id        String   @id @default(uuid())
  rating    Int      @default(5)
  comment   String?  @db.Text
  images    String[]
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  productId String
  product   Product  @relation(fields: [productId], references: [id])
  createdAt DateTime @default(now())
}